[project]
name = "video-transcriber"
version = "0.1.0"
description = "Local video transcription application"
requires-python = ">=3.11"

dependencies = [
    "PySide6",
    "yt-dlp",
    "faster-whisper",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]